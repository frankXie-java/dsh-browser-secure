node_modules/
**/node_modules/
**/lib/
!benchmark/lib/
!benchmark/lib/**
**/dist/
**/dist-firefox/

# Local-only secrets (PAT etc.) — never commit
.env

# Extension signing artifacts — the .pem is a PRIVATE KEY, never commit it.
*.crx
*.pem
